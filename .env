DATABASE_URL="file:./database.db"
JWT_SECRET="bait-al-mandi-production-secret-change-me-2026"
NEXT_PUBLIC_SITE_URL="http://menu.local"

# Next.js 15: allow all LAN origins in dev mode (192.168.x.x, 10.x.x.x, etc.)
NEXT_ALLOWED_DEV_ORIGINS=*

# Bind to all network interfaces (LAN access)
HOSTNAME=0.0.0.0
PORT=3000
